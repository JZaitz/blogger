<h1>List of Tags</h1>

<ul id="tags">
  <% @tag.each do |tag| %>
  <li>
    <%= link_to tag.name, tag_path(tag) %>
    <%= link_to 'Delete', tag_path(tag), method: :delete,
      data: {confirm: "Confirm Delete Tag"} %>
  </li>
  <% end %>
</ul>

<%= link_to "Back to Articles", articles_path %>
